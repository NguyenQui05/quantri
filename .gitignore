# Vercel (chứa project/org ID nội bộ)
.vercel

# macOS
.DS_Store

# Log runtime
*.err
*.log

# Dependencies
node_modules

# File môi trường bí mật (KHÔNG BAO GIỜ commit)
.env
.env.*
*.key
*.pem

# File chứa dữ liệu khách hàng thật (tên, SĐT...) để nhập tay vào Supabase — KHÔNG commit vì repo đang public
leads_import_*.sql
*_import_*.sql
